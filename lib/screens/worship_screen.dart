import 'package:flutter/material.dart';

class WorshipScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Worship'),
      ),
      body: Center(
        child: Text('Content for Worship'),
      ),
    );
  }
}
