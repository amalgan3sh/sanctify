import 'package:flutter/material.dart';

class OrganizationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Organization'),
      ),
      body: Center(
        child: Text('Content for Organization'),
      ),
    );
  }
}
