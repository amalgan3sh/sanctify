import 'package:flutter/material.dart';

class PublicationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Publication'),
      ),
      body: Center(
        child: Text('Content for Publication'),
      ),
    );
  }
}
