import 'package:flutter/material.dart';

class BishopAndVicarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Bishop & Vicar'),
      ),
      body: Center(
        child: Text('Content for Bishop & Vicar'),
      ),
    );
  }
}
