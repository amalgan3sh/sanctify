import 'package:flutter/material.dart';

class OfficeBearersScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Office Bearers'),
      ),
      body: Center(
        child: Text('Content for Office Bearers'),
      ),
    );
  }
}
