import 'package:flutter/material.dart';

class ParishDirectoryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Parish Directory'),
      ),
      body: Center(
        child: Text('Content for Parish Directory'),
      ),
    );
  }
}
